extends layout

append title
  title #{name} | #{type} | #{siteName}

block heading
  h1 #{type}: #{name}

block content
  .col-1-2
    h2 Configuration
    p 
      | You may edit the options object below and re-analyze the fixture to update the results. For more info, see#{' '}
      a(href="https://github.com/dequelabs/axe-core/blob/master/doc/API.md#b-options-parameter") the aXe documentation for the Options parameter
      | .
      
    label(for="config-textarea") Options Object
    textarea#config-textarea(rows="8")
    button#analyze(type="button") Analyze
    
    h2 Fixture
    h3 Rendered View
    p Everything in the area below will be targeted by aXe when you analyze the page.
    #fixture
      block fixture
      
    h3 Markup
    pre#markup.prettyprint(readonly="true")
    
  .col-1-2
    h2 Results
    
    label(for="axe-results") Violations Array
    textarea#axe-results(rows="30" readonly="true")

  script(src=relPath+"analyze.js")
  script.
    analyze();
